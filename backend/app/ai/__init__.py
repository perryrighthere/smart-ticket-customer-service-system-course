"""AI helpers for Lesson 5.

This package contains:
- A lightweight ticket classifier (TF-IDF + linear model with safe fallback)
- A simple LLM client wrapper with graceful degradation
- A service helper to generate AI suggestions for tickets
"""

